defaults:
  - default
  - override runtime: full_gpu_faiss_null_alpha01_min1000
  - _self_

analysis:
  null_distribution:
    draws_per_batch: 200
    min_total_draws: 1000
    max_total_draws: 5000
    adaptive_stop:
      enabled: true
      alpha: 0.01
      correction: bh_fdr
      require_all_hypotheses: true
